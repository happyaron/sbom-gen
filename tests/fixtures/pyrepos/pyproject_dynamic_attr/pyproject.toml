[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dynamicattrpkg"
dynamic = ["version"]

[tool.setuptools.dynamic]
version = { attr = "dynamicattrpkg.__version__" }
